#include <stdio.h>
#include <stdlib.h>
int main()
{
 int n, i;
 int *c;
 printf("Enter number of elements: ");
 scanf("%d", &n);
 c = (int *)calloc(n, sizeof(int));
 printf("\n\nValues after calloc:\n");
 for (i = 0; i < n; i++)
 printf("%d ", c[i]);
 system("getmac");
 return 0;
}
